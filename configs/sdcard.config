image sdcard.img {
	hdimage {
	}

	partition xipImage {
		image = "kernel.bin"
	}

	partition dtb {
		image = "dtb.bin"
	}

        partition rootfs {
                image = "rootfs.ext2"
        }
}
